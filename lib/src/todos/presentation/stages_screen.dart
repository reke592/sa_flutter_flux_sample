import 'package:flutter/material.dart';

class StagesScreen extends StatelessWidget {
  static const routeName = '/stages';
  const StagesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Stages Screen'),
    );
  }
}
